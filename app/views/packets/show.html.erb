<% title "Packet" %>

<p>
  <strong>User:</strong>
  <%= @packet.user_id %>
</p>

<p>
  <%= link_to "Edit", edit_packet_path(@packet) %> |
  <%= link_to "Destroy", @packet, :confirm => 'Are you sure?', :method => :delete %> |
  <%= link_to "View All", packets_path %>
</p>
